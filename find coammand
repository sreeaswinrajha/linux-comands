find command is used to find the files saved in the system
command : find -name hlo.txt
it will show the where the hlo.txt is located if in any user or directory it might be 
locate is used to find in same directory

to avoid errer of permison denined we ca use : 2>/dev/null
where null is like an dustbin

command : find -name (filename which we want) 2>/dev/null  (no gap if we use 2> in it)

also e can use find / -name hlo.txt 
